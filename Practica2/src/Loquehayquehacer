/*
Hay que acordase de crear el directorio mount-point

con la opción mkdir " mount-point "


Prototipo de la función RM

my_unlink (fuselib.c):

static int my_unlink(const char *path){
	fprint(stderr,"-----> my_unlink, path %s\n", path);
	int idxFile = findFileByName(&myFileSystem, (char*)path+1);
	
	if(idxFile== -1)
		return -ENOENT; //fichero no encontrado
		
	int idxNode  = myFileSystem.directory.files[idxFile].nodeIdx;
	
	return 0;
}
Poner otra cosa
Anadimos una linea para probar
Anadimos otra linea distinta


*/